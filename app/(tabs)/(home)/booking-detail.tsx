// S11 route in the (home) stack — reached from Home (S03). The screen itself is
// the shared component; see components/BookingDetailScreen.tsx for why it lives
// there and is registered as a route in two tab groups.
export { default } from '@/components/BookingDetailScreen';
