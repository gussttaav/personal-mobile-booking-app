// S11 route in the (booking) stack — reached from Success (S08) so "back" pops
// to S08 rather than jumping tabs to Inicio. Same shared screen as the (home)
// route; see components/BookingDetailScreen.tsx.
export { default } from '@/components/BookingDetailScreen';
